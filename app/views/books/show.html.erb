<body>
 
<% @books.each do |book| %>
 <table> 
  <td><%= link_to "Edit", edit_book_path(book.id)%></td>
  <td><%= link_to "Back", book_path(book.id)%></td>
 </table>
<% end %>
 
<%= form_with model: @book, url: '/books', method: :post do |f| %>
 <p id="notice"></p>
<% end %>
  
</body>
